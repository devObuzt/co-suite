# Client Quotes

Client quote files live in this folder.

Use `client-quote-template.md` for each new proposal, then save a dated copy using:

```txt
YYYY-MM-DD-client-project-quote.md
```

Every quote must connect back to `../registers/estimation-register.md` and follow `../workflows/client-quote-lifecycle.md`.
