# Architecture Review Log

| Date | Review ID | Scope | Planned Architecture | Actual Implementation | Missing Work | Required Action | Owner | Status |
|---|---|---|---|---|---|---|---|---|
| 2026-06-07 | ARCH-REV-M1-001 | co-Suite M1 baseline: Suite Memory, onboarding, generation jobs, media, publishing, analytics, provider limits | Suite Memory is system of record; generation and publishing run as explicit workflows/jobs; media artifacts have durable public URLs and clear state; integrations expose capability readiness | Suite Memory exists as `Suite.brand`/`Suite.strategy` JSON; `GenerationJob` exists but execution uses FastAPI background tasks; media storage supports R2/local fallback; publishing is request lifecycle with partial result ambiguity; analytics/connections infer readiness from JSON | Suite Memory v0 contract, durable/stuck job handling, media readiness metadata, per-platform publish result state, typed analytics/connection readiness | Use `docs/software-company/projects/cosuite/architecture-baseline-m1.md` as the implementation baseline; require Architecture re-check after generation/media/Suite Memory changes | Architecture + Developers Manager | open |
| 2026-06-07 | ARCH-REV-M1-002 | co-Suite M1 first implementation slice: Suite Memory v0, generation job status metadata, media readiness | Stabilize the first read contracts without a broad rewrite; keep remaining M1 release blockers visible | Suite Memory v0 is exposed as a derived read contract; generation job serialization includes wait/stale/provider metadata and process-local execution warning; content list responses include media readiness with public/local/missing/failed states | Generation and product bulk still execute through FastAPI background tasks; provider-limit jobs are not retried; media readiness is not enforced before publish; partial publish can still mark global content as published; generation still reads raw `Suite.brand`/`Suite.strategy` directly | Accept Suite Memory v0 and job/media response contracts for Slice 01, but require publish preflight/per-platform result fixes and an accepted queue/stuck-job posture before M1 release candidate | Architecture + Developers Manager + DevOps | revise_before_rc |
